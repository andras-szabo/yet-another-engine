TODO next:

= finish AssetDatabase::Populate

- attach .meta to the extension: "PXL_123.jpg.meta" &c; so that should solve
  the multiple asset names (bar the exception) problem

- hook up editor progress into pop_adb somehow?

----
Asset database:
  - implement "AssetDatabase::PopulateFromFolder", leave it open for future
    partial importing, where you can filter which folders to _actually_ open.
	- save previous maps so that on exception or error we can restore them
	- clear the current maps
    	- recursively walk the (filtered) path tree.
		- for each source file:
			- check if a corresponding meta file is present.
				- if it is not, create one
			=> so now we have a source asset and a meta file.
			   Check if there's a GUID clash. If there is, log a warning,
			   then generate a new GUID; log a warning, and save the
			   change (that a new GUID was generated for asset X) in some
			   structure keeping track of how successful the import was.
	- log the "this is what happened on import" thing

  - expose AssetDatabase as an engine-core service.

----

  o Editor starts running in an endless loop, waiting for a console command.
  o If the command is "quit", we stop.
  ... If the command is "createProject <path/to/project/folder>":
  	o create path/to/project/folder
	o copy into it CMakeLists.txt
	o copy into it "src"
	... generate the project in <path/to/project/folder>

	=> this should be:
	(from within the game-template folder)
	cmake -B build -G "Visual Studio 17 2022" -DENGINE_CORE_SDK_DIR=C:\Dev\EngineSdk\sdk
	cmake --build build --config debug

		=> 1.) confirm that this works:
			o call cproj and confirm it creates the folder
			o run the cmdline above and see what happens.

